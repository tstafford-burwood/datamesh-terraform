# ---------------------------------------------------------
# STEP 1:
# CHANGE AFTER THE AUTOMATION PROJECT IS UP AND HAS A BUCKET AND PREFIX
# ---------------------------------------------------------

terraform_state_bucket = "" // CHANGE BEFORE FIRST DEPLOYMENT
terraform_state_prefix = "" // CHANGE BEFORE FIRST DEPLOYMENT

# ---------------------------------------------------------
# STEP 2:
# CHANGE AFTER PACKER PROJECT IS UP
# ---------------------------------------------------------

srde_packer_project_id = "" // CHANGE AFTER PACKER PROJECT IS UP

# ---------------------------------------------------------
# STEP 3
# CHANGE AFTER STAGING AND CLOUD COMPOSER ARE UP
# ---------------------------------------------------------

srde_composer_dag_bucket = "" // CHANGE AFTER CLOUD COMPOSER IS UP

# ---------------------------------------------------------
# CHANGE BASED ON WHAT VSC IS BEING USED.
# These pipelines and code below is for BitBucket
# ---------------------------------------------------------

srde_plan_trigger_repo_name  = "" // CHANGE BEFORE FIRST DEPLOYMENT
srde_plan_branch_name        = "" // CHANGE BEFORE FIRST DEPLOYMENT
srde_apply_trigger_repo_name = ""
srde_apply_branch_name       = "^master$"